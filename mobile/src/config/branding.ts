/**
 * Brend – web (mobile-pwa) bilan bir xil nom va o‘lchamlar.
 */
export const BRAND = {
  /** To‘liq nomi – login va header da */
  name: 'FB DISTRIBUTORS GROUP',
  /** Qisqa nom (FB Warehouse va hokazo) */
  shortName: 'FB Warehouse',
  /** Login sahifasidagi logo balandligi (web: h-20 sm:h-24 ≈ 80–96px) */
  loginLogoSize: 96,
  /** Header dagi logo o‘lchami (web: h-8 = 32px) */
  headerLogoSize: 32,
} as const;
