# Release signing for Play Store. Copy to keystore.properties and fill in.
# Create keystore once: keytool -genkeypair -v -storetype PKCS12 -keystore my-release-key.keystore -alias my-key-alias -keyalg RSA -keysize 2048 -validity 10000
# Put the .keystore file in this folder (android/app/).

storeFile=my-release-key.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=my-key-alias
keyPassword=YOUR_KEY_PASSWORD
