import 'package:flutter/material.dart';

/// Branding aligned with React Native `branding.ts` / web PWA.
class Brand {
  Brand._();

  static const String name = 'FB DISTRIBUTORS GROUP';
  static const String shortName = 'FB Warehouse';
  static const double loginLogoSize = 96;
  static const double headerLogoSize = 32;

  /// Inventory list header / global light `AppBar` (matches `InventoryScreen` top bar).
  static const Color pickerHeaderNavy = Color(0xFF0D1B2A);
}
